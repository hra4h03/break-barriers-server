<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Request to Admin</title>
</head>
<body>
  <div style="padding: 20px 10px;">

    <h2>New People request to join <i><%= room.title %></i></h2>
    <ul>

      <% room.waitlist.forEach(({ username }) => { %>
        <li><%= username %> </li>
      <% }) %> 
    </ul>
    <button style="padding: 8px 14px; background-color: darkblue; color: white; outline: none;">
      Check them
    </button>
  </div>
</body>
</html>